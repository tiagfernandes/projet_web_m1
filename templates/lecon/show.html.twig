{% extends 'base.html.twig' %}

{% block title %}Lecon{% endblock %}

{% block body %}
    <h1>Lecon</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ lecon.id }}</td>
            </tr>
            <tr>
                <th>Present</th>
                <td>{{ lecon.present ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_lecon_index') }}">back to list</a>

    <a href="{{ path('app_lecon_edit', {'id': lecon.id}) }}">edit</a>

    {{ include('lecon/_delete_form.html.twig') }}
{% endblock %}
