<a id="show-sidebar" class="btn btn-sm btn-dark" href="#">
    <i class="fas fa-bars"></i>
</a>
<nav id="sidebar" class="sidebar-wrapper">
    <div class="sidebar-content">
        <div class="sidebar-brand">
            <a href="{{ path('app_entrainement_today') }}">CEMVS</a>
            <div id="close-sidebar">
                <i class="fas fa-times"></i>
            </div>
        </div>
        <div class="sidebar-header">
            <img src="{{ asset('assets/img/logo_MVDS.PNG') }}" alt="Logo" width="150">
        </div>
        <div class="sidebar-header">
            <a href="{{ path('app_profile_edit') }}">
                <div class="user-pic">
                    <img class="img-responsive img-rounded"
                         src="https://raw.githubusercontent.com/azouaoui-med/pro-sidebar-template/gh-pages/src/img/user.jpg"
                         alt="User picture">
                </div>
                <div class="user-info">
                  <span class="user-name">{{ app.user.firstName }}
                      <strong>{{ app.user.lastName }}</strong>
                  </span>
                    <span class="user-role">{% if app.user.groupe is defined %}{{ app.user.groupe.name }}{% else %}{{ app.user.roles[0] }}{% endif %}</span>
                    <span class="user-status">
                        <i class="fa fa-circle"></i>
                        <span>Online</span>
                    </span>
                </div>
            </a>
        </div>
        <!-- sidebar-search  -->
        <div class="sidebar-menu">
            <ul>
                <li class="header-menu">
                    <span>Général</span>
                </li>
                <li class="sidebar-dropdown">
                    <a href="#">
                        <i class="fas fa-trophy"></i>
                        <span>Compétition</span>
                    </a>
                    <div class="sidebar-submenu">
                        <ul>
                            {% if is_granted('ROLE_ADMIN') %}
                                <li>
                                    <a href="{{ path('competition_index') }}">Liste des compétitions</a>
                                </li>
                            {% elseif is_granted('ROLE_TIREUR') %}
                                <li>
                                    <a href="{{ path('app_competition_registration') }}">S'inscrire à une
                                        compétition</a>
                                </li>
                                <li>
                                    <a href="{{ path('app_competition_registed') }}">Mes inscriptions</a>
                                </li>
                            {% endif %}
                        </ul>
                    </div>
                </li>
                <li class="sidebar-dropdown">
                    <a href="#">
                        <i class="fas fa-running"></i>
                        <span>Entraînements et leçons</span>
                        {#<span class="badge badge-pill badge-danger">3</span>#}
                    </a>
                    <div class="sidebar-submenu">
                        <ul>
                            {% if is_granted('ROLE_ADMIN') %}
                                <li>
                                    <a href="{{ path('app_entrainement_newyear') }}">Ajouter un entraînement au
                                        planning</a>
                                </li>
                                <li>
                                    <a href="{{ path('app_entrainement_new') }}">Ajouter un entraînement</a>
                                </li>
                            {% endif %}
                            <li>
                                <a href="{{ path('app_entrainement_today') }}">Entraînements du jour</a>
                            </li>
                            <li>
                                <a href="{{ path('app_entrainement_mesentrainements') }}">
                                    {% if is_granted('ROLE_ADMIN') %}
                                        Les Entraînements finis
                                    {% else %}
                                        Mes Entraînements finis
                                    {% endif %}
                                </a>

                            </li>
                            <li>
                                <a href="{{ path('app_entrainement_index') }}">
                                    Tous les entraînements
                                </a>
                            </li>
                            <li>
                                <a href="{{ path('app_lecon_index') }}">
                                    {% if is_granted('ROLE_ADMIN') %}
                                        Toutes les leçons
                                    {% else %}
                                        Mes leçons
                                    {% endif %}

                                </a>
                            </li>
                        </ul>
                    </div>
                </li>
                {% if is_granted('ROLE_ADMIN') %}
                    <li class="sidebar-dropdown">
                        <a href="#">
                            <i class="fas fa-users"></i>
                            <span>Groupe</span>
                        </a>

                        <div class="sidebar-submenu">
                            <ul>
                                <li>
                                    <a href="{{ path('groupe_index') }}">Liste des groupes</a>
                                </li>
                                <li>
                                    <a href="{{ path('groupe_new') }}">Ajouter un groupe</a>
                                </li>
                            </ul>
                        </div>
                    </li>
                {% endif %}
                {% if is_granted('ROLE_MAITRE') %}
                    <li class="sidebar-dropdown">
                        <a href="#">
                            <i class="fas fa-users"></i>
                            <span>Groupe</span>
                        </a>

                        <div class="sidebar-submenu">
                            <ul>
                                <li>
                                    <a href="{{ path('groupe_index') }}">Liste des groupes</a>
                                </li>
                            </ul>
                        </div>
                    </li>
                {% endif %}
                {% if is_granted('ROLE_ADMIN') %}
                    <li class="sidebar-dropdown">
                        <a href="#">
                            <i class="fas fa-user-tie"></i>
                            <span>Maîtres d'armes</span>
                        </a>
                        <div class="sidebar-submenu">
                            <ul>

                                <li>
                                    <a href="{{ path('maitre_armes_index') }}">Liste des maîtres d'armes</a>
                                </li>
                                <li>
                                    <a href="{{ path('maitre_armes_new') }}">Ajouter un maître d'armes</a>
                                </li>

                            </ul>
                        </div>
                    </li>
                {% endif %}
                <li class="sidebar-dropdown">
                    <a href="#">
                        <i class="far fa-check-square"></i>
                        <span>Objectif</span>
                    </a>
                    <div class="sidebar-submenu">
                        <ul>
                            <li>
                                <a href="{{ path('objectif_index') }}">Liste des objectifs</a>
                            </li>
                            <li>
                                <a href="{{ path('objectif_new') }}">Ajouter un objectifs</a>
                            </li>
                        </ul>
                    </div>
                </li>
                {% if is_granted('ROLE_ADMIN') %}
                    <li class="sidebar-dropdown">
                        <a href="#">
                            <i class="fas fa-user"></i>
                            <span>Tireurs</span>
                        </a>
                        <div class="sidebar-submenu">
                            <ul>

                                <li>
                                    <a href="{{ path('tireur_index') }}">Liste des tireurs</a>
                                </li>
                                <li>
                                    <a href="{{ path('tireur_new') }}">Ajouter un tireur</a>
                                </li>

                            </ul>
                        </div>
                    </li>
                {% endif %}
                {% if is_granted('ROLE_TIREUR') %}
                    <li class="sidebar-dropdown">
                        <a href="#">
                            <i class="far fa-chart-bar"></i>
                            <span>Statistiques</span>
                            {#<span class="badge badge-pill badge-danger">3</span>#}
                        </a>
                        <div class="sidebar-submenu">
                            <ul>
                                <li>
                                    <a href="{{ path('app_statistic_index') }}">Consulter les statistiques</a>
                                </li>
                            </ul>
                        </div>
                    </li>
                {% endif %}
            </ul>
        </div>
        <!-- sidebar-menu  -->
    </div>
    <!-- sidebar-content  -->
    <div class="sidebar-footer">
        {#        <a href="#">#}
        {#            <i class="fa fa-bell"></i>#}
        {#            <span class="badge badge-pill badge-warning notification">3</span>#}
        {#        </a>#}
        {#        <a href="#">#}
        {#            <i class="fa fa-envelope"></i>#}
        {#            <span class="badge badge-pill badge-success notification">7</span>#}
        {#        </a>#}
        {#        <a href="#">#}
        {#            <i class="fa fa-cog"></i>#}
        {#            <span class="badge-sonar"></span>#}
        {#        </a>#}
        <a href="{{ path('logout') }}">
            <i class="fa fa-power-off"></i>
        </a>
    </div>
</nav>