<form method="post" action="{{ path('maitre_armes_delete', {'id': maitre_arme.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ maitre_arme.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
